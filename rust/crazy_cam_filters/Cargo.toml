[package]
name = "crazy_cam_filters"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
uniffi = { version = "0.28", features = ["build", "cli"] }
image = "0.25"
uniffi_bindgen = "0.28"

[build-dependencies]
uniffi = { version = "0.28", features = ["build"] }